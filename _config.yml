future: true

collections:
  course:
    output: true
    permalink: /kurzy/:slug
  event:
    output: true
    permalink: /terminy/:slug

defaults:
  - scope:
      path: ""
      type: "course"
    values:
      layout: "course"
  - scope:
      path: ""
      type: "event"
    values:
      layout: "event"
